---
- name: Add IP address of all hosts to all hosts
  lineinfile:
    dest: /etc/hosts
    regexp: ".*{{ item }}$"
    line: "{{ item.ip }} {{ item.hostname }}"
    state: present
  with_items: "{{ rpi_cluster }}"
